# `icicl` external benchmark

Reserved for an optional, post-v0.1 integration with the public experiments
from *Transformers for dynamical systems learn transfer operators in-context*.

This benchmark is deliberately outside the SpiralLens core. Core APIs,
calibration thresholds, and candidate contracts must be frozen before this
benchmark is added, so the instrument is not tuned to the paper's
transfer-operator result.
